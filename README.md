# LegalPDFExtractor
Legal contracts are predominantly distributed as PDFs, but converting them into structured, machine-readable formats remains error-prone. Naïve PDF-to-text extraction breaks clause boundaries, loses section hierarchy, mixes headers and footers into content, and produces chunks that are unusable for downstream LLM tasks
